# テスト実行記録 — 2026-08-24 07:50

* 記録者フェーズ: P103 (結合テスト実行) / P201 (受け入れ結合テスト実行) をまとめて記録
* 対象ブランチ/コミット: `main` (未コミット、作業ツリー)
* 実行環境: Windows 11 / Python 3.14.2 / Node v24.12.0 / Chrome Headless 151
  * **`sadf` なし** (Windows に sysstat が無く、WSL の `sudo` がパスワードを要求するため導入不可)
  * **Docker なし**

## 集計

| 区分 | 実行 | PASS | FAIL | NOT RUN |
| --- | --- | --- | --- | --- |
| バックエンド単体 (P007 各スプリント) | 154 | 154 | 0 | 2 |
| 結合テスト (T001〜T006) | 45 | 45 | 0 | 8 |
| 受け入れテスト (A002〜A004) | 11 | 11 | 0 | 1 |
| フロントエンド単体 (U006/U007) | 37 | 37 | 0 | 0 |
| **合計** | **247** | **247** | **0** | **11** |

* **NOT RUN の 11 件は合格に数えていない。** 内訳は各ブロックに記載する。
* スイート全体を 2 回続けて実行し、同じ結果になることを確認した (REQ-N-014)。

---

## T001: sar経路の結合

- 実行日時: 2026-08-24 07:45
- 実行者: Executor (P103)
- 対象ブランチ/コミット: main (作業ツリー)
- 参照したテスト指示: `docs/P008-test-direction/T001-sar-pipeline.md`
- 実行環境: Python 3.14.2 / `sadf` なし
- 事前ビルド結果: U001〜U005 の単体テスト合格
- 実行コマンド: `python -m pytest backend/tests/integration/test_t001_sar_pipeline.py -q`
- 結果: **PASS** (9 件)
- 期待結果: 実データ `sar15`〜`sar23` の 9 件が 200 を返し、`sar23` が 21 グループを持ち、全系列で INV-1 が成立し、`MG-CPU` の `timestamps` が 140 件以上であること
- 実際の結果:
  - 9 件すべて 200。
  - `sar23` の `groups` = **21 件**。`groupId` 集合が期待集合と一致。
  - `MG-CPU` の `timestamps` = **143 件**。ブロックが切れている兆候 (11 件・13 件) は無し。
  - `MG-CPU` のキー = `{all, 0, 1, 2}`、`MG-NET` のキー数 = 22 (20 以上)。
  - `Average` 由来の値の混入なし。
  - `hostname=www4250uj` / `cpuCount=3` / `arch=x86_64` / `date=2026-08-23`。
  - `MG-CPU` の `all/%usr` 先頭値 = 2.07 (実ファイル記載値と一致)。
- 失敗ログ抜粋: —
- 再現手順: —
- 影響がありそうなモジュール: —
- テスト指示との矛盾の疑い: **あり**(矛盾する上位文書の箇所: `docs/P001-requirement.md` §7 のメトリクスグループ一覧が 18 グループであったが、実データには PSI (Pressure Stall Information) の 3 グループ (`%scpu-*` / `%sio-*` / `%smem-*`) が存在した)
- 次工程への申し送り: P001 §7 の一覧は実データの調査時に PSI を取りこぼしていた。REQ-F-012「格納されている時系列データが全てグラフ表示される」を満たすには必要なため、実装で 3 グループを追加し 21 グループとした。P001 §7・P002 §5.4・P007/P008 の該当記述を 21 に更新済み。

---

## T002: 2経路の一致 (観点A)

- 実行日時: 2026-08-24 07:45
- 実行者: Executor (P103)
- 参照したテスト指示: `docs/P008-test-direction/T002-dual-path-equivalence.md`
- 実行環境: **`sadf` なし**
- 実行コマンド: `python -m pytest backend/tests/integration/test_t002_dual_path.py -q`
- 結果: **NOT RUN** (8 件すべて)
- 期待結果: 同一日の `sa{DD}` と `sar{DD}` の応答が、グループ集合・キー・時刻・数値 (絶対誤差 0.01 以内) で一致すること
- 実際の結果: **実行できなかった。** `sadf` がこの環境に無く、導入もできないため。スキップ理由に「NOT RUN: この環境に sadf が無く導入もできないため実行できない。合格として扱わないこと。」を明記してある。
- 失敗ログ抜粋: — (実行できず情報なし)
- 再現手順: — (同上)
- 影響がありそうなモジュール: `app/readers/sa_binary.py` / `app/metrics/catalog.py` の `SADF_*` 対応表
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: **本テストは本フェーズの最重要テストであり、未実行のまま残っている。** `sadf` が利用可能な環境で最初に実行すること。不一致が出た場合、その性質 (指標名の表記ゆれ / グループ欠落 / 時刻ずれ / 数値の桁違い) を分類して記録すれば、修正箇所は `app/metrics/catalog.py` の対応表 1 箇所に絞れる設計になっている。

---

## T003: カタログAPIの結合

- 実行日時: 2026-08-24 07:45
- 参照したテスト指示: `docs/P008-test-direction/T003-catalog-api.md`
- 実行環境: `sadf` なし → 一覧は `sar` 9 件のみになる条件で判定
- 実行コマンド: `python -m pytest backend/tests/integration/test_t003_catalog_api.py -q`
- 結果: **PASS** (7 件)
- 期待結果: 期間全体で `totalItems=9` (`sadf` なしのため)、並び順が日付昇順→`sa`→`sar`、境界日を含む、ページ境界、0 件時 `totalPages=0`、超過ページで 200 かつ空配列、応答が camelCase、health の件数と一覧が整合
- 実際の結果: すべて期待どおり。ページ境界は `tmp_path` にコピーを作り 24 件規模にして検証し、2 ページ目の件数が一致した。実データディレクトリは書き換えていない。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: `sadf` がある環境では `totalItems=18` になる。テストは `sadf_available()` で期待値を切り替える実装にしてある。

---

## T004: エラー系とパストラバーサル

- 実行日時: 2026-08-24 07:45
- 参照したテスト指示: `docs/P008-test-direction/T004-error-and-traversal.md`
- 実行コマンド: `python -m pytest backend/tests/integration/test_t004_errors.py -q`
- 結果: **PASS** (17 件)
- 期待結果: パラメータ異常 7 種が 400 / `INVALID_PARAMETER`、不正 `fileId` 7 種が 404 / `FILE_NOT_FOUND`、エラー応答の形式、日本語メッセージ、`sadf` 不在時の 503 と `hint`
- 実際の結果: すべて期待どおり。
  - **パストラバーサル系 (`../../etc/passwd` / `/etc/passwd` / 命名規則外 / 不正 base64) はいずれも 404。** 404 以外を返したものは無い。
  - `sadf` 不在時に `sa23` を指定すると 503 / `SADF_UNAVAILABLE`、`hint` に `sar23` が含まれることを確認。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: なし

---

## T005: キャッシュ効果

- 実行日時: 2026-08-24 07:45
- 参照したテスト指示: `docs/P008-test-direction/T005-cache-effect.md`
- 実行コマンド: `python -m pytest backend/tests/integration/test_t005_cache.py -q`
- 結果: **PASS** (5 件)
- 期待結果: 一覧 2 回目で採取日の再読み取りが 0 回、メトリクス 2 回目でリーダ呼び出しが 0 回、`mtime` / `size` 変更で再読み取りが発生、キャッシュ命中時 1 秒未満
- 実際の結果: すべて期待どおり。呼び出し回数による検証を主とし、所要時間は補助として確認した。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: なし

---

## T006: 構造化ログ

- 実行日時: 2026-08-24 07:45
- 参照したテスト指示: `docs/P008-test-direction/T006-structured-log.md`
- 実行コマンド: `python -m pytest backend/tests/integration/test_t006_logging.py -q`
- 結果: **PASS** (6 件)
- 期待結果: 各行が単独で JSON として解釈でき、`ts`/`level`/`event`/`message` を持ち、リクエストログに `method`/`path`/`status`/`durationMs` があり、404 も記録され、読み取り失敗が `reader.failed` として記録されること
- 実際の結果: すべて期待どおり。メッセージ中の改行は JSON 内で `\n` にエスケープされ、1 レコードが 1 行に収まることを確認した。`sadf` 不在により `sa` 9 件について `reader.failed` (`reason=SADF_UNAVAILABLE`) が記録された。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: なし

---

## A001: 通し操作と状態復元 (観点C)

- 実行日時: 2026-08-24 07:48
- 実行者: Reviewer Loop (P201)
- 参照したテスト指示: `docs/P009-acceptance-direction/A001-end-to-end-navigation.md`
- 実行環境: Chrome Headless 151 (Karma)
- 実行コマンド: `npm --prefix frontend test`
- 結果: **PASS** (コンポーネントテストによる代替実行)
- 期待結果: 初期値が実行月の 1 日と末日、2 ページ目へ移動でき先頭行が選択され、SC-02 で見出し・説明・グラフが表示され、戻ると期間・一覧・ページ番号・選択行が復元され、戻り時に `GET /api/log-files` が再実行されないこと
- 実際の結果:
  - 初期値: 31 日月 / 30 日月 / うるう年 2 月 (29 日) / 平年 2 月 (28 日) の 4 ケースで確認。
  - ページ切り替えで `page=2` が送られ、切り替え先の先頭行が選択されることを確認。
  - 検索実行で `page=1` に戻ることを確認。
  - **保持状態がある場合、初期表示で `GET /api/log-files` が呼ばれず、期間・ページ番号・選択行が復元されることを確認** (`http.expectNone` で API 非実行を検証)。
  - SC-02 で見出しと説明が DOM 上でグラフ要素より前に現れることを確認。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: **ブラウザ自動操作 (E2E) ハーネスは導入していない。** ルータを介した実遷移ではなく、`SearchStateService` を共有するコンポーネントテストで復元を検証した。指示書が許容する代替手段だが、実ブラウザでの通し操作は未検証である。

---

## A002: sarのみでの運用

- 実行日時: 2026-08-24 07:48
- 参照したテスト指示: `docs/P009-acceptance-direction/A002-sar-only-operation.md`
- 実行環境: **`sadf` なし** — この環境の実態がそのまま検証条件になる
- 実行コマンド: `python -m pytest backend/tests/acceptance/test_a002_sar_only.py -q`
- 結果: **PASS** (7 件)
- 期待結果: 起動が失敗しない、`status="ok"` / `sadfAvailable=false` / `readable=9` / `unreadable=9`、一覧に `sar` 9 件のみ、`sar` のグラフデータ取得可、`sa` 直接指定で 503 + `hint`、一覧が 5xx にならないこと
- 実際の結果: すべて期待どおり。**`sadf` 不在がアプリ全体の異常として扱われていない**ことを確認した (REQ-N-015)。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: なし

---

## A003: 再起動耐性

- 実行日時: 2026-08-24 07:48
- 参照したテスト指示: `docs/P009-acceptance-direction/A003-restart-resilience.md`
- 実行環境: Docker なし → **ローカルプロセスとして起動・停止・再起動**して確認
- 実行コマンド: `python -m pytest backend/tests/acceptance/test_a003_restart.py -q`
- 結果: **PASS** (1 件)
- 期待結果: 2 回目・3 回目の起動が成功し、応答が 1 回目と完全に一致すること
- 実際の結果: 別プロセスとして 3 回起動し、`sadfAvailable` / `readableFileCount` / `totalItems` / グループ数 / `groupId` 集合 / `MG-CPU` の先頭値 / `timestamps` 件数 がすべて 3 回とも同一であった。キャッシュを失った状態から毎回再構築できている。
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: **コンテナの再起動 (`docker compose restart`) による確認は行えていない** (A005 の対象)。永続データストアが無くマイグレーションも存在しないため、構造上の起動失敗要因は無い。

---

## A004: 性能

- 実行日時: 2026-08-24 07:48
- 参照したテスト指示: `docs/P009-acceptance-direction/A004-performance.md`
- 実行コマンド: `python -m pytest backend/tests/acceptance/test_a004_performance.py -q -s`
- 結果: **PASS (3 項目) / NOT RUN (1 項目)**
- 期待結果と実際の結果:

| # | 指標 | 基準 | 実測 (P95) | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `GET /api/log-files` (400 件超規模) | < 1 秒 | **0.051 秒** (min 0.022 / median 0.042 / max 0.055) | PASS |
| 2 | `GET .../metrics` 初回 | < 5 秒 | **0.108 秒** (min 0.077 / median 0.085 / max 0.110) | PASS |
| 3 | `GET .../metrics` キャッシュ命中 | < 1 秒 | **0.013 秒** (min 0.007 / median 0.009 / max 0.013) | PASS |
| 4 | SC-02 の最初のグラフ描画 | < 3 秒 | — | **NOT RUN** |

- 失敗ログ抜粋: —
- 再現手順: —
- 影響がありそうなモジュール: —
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: 項目 4 (REQ-N-005) は**この環境にブラウザ描画の計測手段が無いため測定していない。合格に数えていない。** 実ブラウザで `sar23` (ネットワークインタフェース 22 本、約 30 グラフ) を開いて計測する必要がある。

---

## A005: コンテナ構成

- 実行日時: 2026-08-24 07:49
- 参照したテスト指示: `docs/P009-acceptance-direction/A005-container-deployment.md`
- 実行環境: **Docker なし**
- 実行コマンド: 実行できず (`docker compose build` / `up` が実行不可)
- 結果: **NOT RUN**
- 期待結果: ビルド成功、SC-01 が表示される、`sadfAvailable=true` かつ `readableFileCount=18`、`sadf -V` が動く、**`sa` ファイルのグラフが表示される**、マウントが読み取り専用、自動再起動、構造化ログ
- 実際の結果: **実行できなかった。** 代替として実行できた範囲は次のみ。
  - `compose.yaml` が YAML として妥当であること: **確認済み**
  - `api` サービスの volumes が `:ro` で終わること: **確認済み**
  - `api` がホストにポートを公開していないこと: **確認済み**
  - イメージのビルド、コンテナ起動、nginx のプロキシ動作、`sadf` の実行可否: **すべて未確認**
- 失敗ログ抜粋: — (実行できず情報なし)
- 再現手順: — (同上)
- 影響がありそうなモジュール: `backend/Dockerfile` / `frontend/Dockerfile` / `frontend/nginx.conf` / `compose.yaml`
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: **本テストは `sa` 経路の唯一の実地検証機会である。** Docker が使える環境で最初に実行し、手順 6 が失敗した場合は `docker compose exec api sadf -j -- -A /var/log/sysstat/sa23 | head -100` の出力を取得すること。それが `docs/P003-backend-spec.md` §9.2 と `app/metrics/catalog.py` の対応表を修正するための一次情報になる。

---

## 実行できなかったテストの一覧 (合格に数えていないもの)

| テストID | 件数 | 実行できなかった理由 |
| --- | --- | --- |
| T002 (2 経路の一致) | 8 | `sadf` がこの環境に無く、導入もできない |
| A004 項目 4 (初回グラフ描画時間) | 1 | ブラウザ描画の計測手段が無い |
| A005 (コンテナ構成) | 全項目 | Docker が無い |
| `test_metrics_api.py::test_収集途中のsa24も200で返る` | 1 | `sadf` が無いため |
| `test_fileid.py::test_ディレクトリ外を指すシンボリックリンクは404` | 1 | Windows でシンボリックリンクを作成できない (管理者権限が必要) |

**上記はいずれも PASS として記録していない。**

---

## TOPO-01: 配布トポロジーの疎通 (P302 実行前チェック項目 7)

- 実行日時: 2026-08-24 07:55
- 実行者: Closing (P302 実行前チェック)
- 参照したテスト指示: なし (`SKILL-P302-deliver.md` の実行前チェック項目 7 にもとづく確認)
- 実行環境: Docker なし。**`frontend/nginx.conf` と同じ経路構成 (静的配信 + `/api/` を書き換えずに前送りするリバースプロキシ + SPA フォールバック) を Python で再現**し、ビルド済み `frontend/dist/sysstatview-frontend/browser` を配信して確認した
- 事前ビルド結果: `npm run build` 成功、バックエンドを `uvicorn` でローカル起動
- 実行コマンド: `python backend/tests/acceptance/_topology_probe.py` (バックエンドを `127.0.0.1:8000` で起動した状態で実行)
- 結果: **PASS** (6 経路すべて)
- 期待結果: フロントエンドが前提とする「同一オリジンで相対パス `/api/...` を呼ぶ」形が、配布トポロジー上で成立すること
- 実際の結果:

| 経路 | 結果 |
| --- | --- |
| `GET /` → `index.html` (静的配信) | HTTP 200、`app-root` を含む |
| `GET /graph/c2FyMjM` → SPA フォールバック | HTTP 200、`index.html` が返る |
| `GET /api/health` → プロキシ | HTTP 200、`status="ok"` |
| `GET /api/log-files?...` → プロキシ | HTTP 200、`totalItems > 0` |
| `GET /api/log-files/{id}/metrics` → プロキシ | HTTP 200、`groups` が非空 |
| `GET /api/metric-catalog` → プロキシ | HTTP 200、`groups` が非空 |

- 失敗ログ抜粋: —
- 再現手順: —
- 影響がありそうなモジュール: —
- テスト指示との矛盾の疑い: なし
- 次工程への申し送り: **`/api/` の前送りでパスが書き換わらないこと**が要点である。`nginx.conf` の `proxy_pass http://api:8000;` は URI 部を持たないため、元の URI (`/api/health`) がそのまま前送りされる。ここに末尾スラッシュを付けて `proxy_pass http://api:8000/;` とすると `/health` に書き換わり、全 API が 404 になる。**nginx.conf を編集する際はこの点に注意すること。**
  なお本確認は nginx 自体ではなく等価な経路構成の再現によるものであり、**nginx の実挙動そのものは未検証**である (A005 の対象)。
