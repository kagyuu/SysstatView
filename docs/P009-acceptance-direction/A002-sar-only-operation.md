あなたはReviewer Loop(実装横断レビュー担当)です。実施後は結果を記録し、停止条件に該当しない限り次のテストタスクへ進んでください。

# 【テストID】A002 — sarのみでの運用

## 【目的】

* `sadf` が利用できない環境でもアプリケーションが正常に成立することを確認する (REQ-N-015)。

## 【参照テスト計画】

* `docs/P006-test-plan.md` §6.2 (この観点は制約を利点に転じられる数少ない箇所であり、必ず検証する)

## 【対象モジュール】

* アプリケーション全体

## 【前提条件】全モジュールビルドが成功していること

* `sadf` が PATH 上に**存在しない**こと。存在する環境で実行する場合は、PATH から外して再現する。

## 【使用するテストデータ】

* 実データディレクトリ (`sa` 9 件 + `sar` 9 件)

## 【事前準備】

* `shutil.which("sadf")` が `None` を返すことを確認し、記録する。

## 【実行手順】

1. バックエンドを起動する。
2. `GET /api/health` を実行する。
3. `GET /api/log-files?from=2026-08-01&to=2026-08-31&perPage=100` を実行する。
4. `sar` の `fileId` で `GET /api/log-files/{fileId}/metrics` を実行する。
5. SC-01 を開き、一覧を確認する。
6. `sar` を選んで SC-02 を表示する。

## 【実行コマンド】

* `python -m pytest backend/tests/acceptance/test_a002_sar_only.py -q`

## 【期待結果】

* 手順1: **起動が失敗しない。**
* 手順2: HTTP 200、`status="ok"`、`sadfAvailable=false`、`sadfVersion=null`。`readableFileCount=9`、`unreadableFileCount=9`。
* 手順3: `totalItems=9`。返る `items` の `kind` がすべて `"sar"`。
* 手順4: HTTP 200 でグラフデータが返る。
* 手順5: 一覧に `sar` 9 件が表示される。**エラー表示が出ない。**
* 手順6: グラフが正常に描画される。
* 参考確認: `sa` の `fileId` を直接指定した場合は 503 / `SADF_UNAVAILABLE` となり、`hint` に同一日の `sar` ファイル名が含まれる。

## 【合否判定基準】

* 上記をすべて満たせば PASS。
* とくに手順1・手順2 で `sadf` 不在が**アプリ全体の異常として扱われた場合 (起動失敗、`status` が `ok` でない、HTTP 5xx)** は FAIL。

## 【失敗時に記録する内容】

* 失敗した手順、期待値、実際の値。

## 【修正禁止事項】

* アプリケーションコードを修正しない。

## 【次タスクへ進む条件】

* 結果を記録したら A003 へ進む。

## 重要:

* アプリケーションコードを修正しないでください。
* テスト失敗時に、その場で修正して再テストしないでください。
* テスト失敗時は、失敗内容をテスト記録に残してください。
